Tracer Slice and Capability Probe

HARDEST FIRST - Riskiest assumption: That we can programmably generate TTS audio, render SVG vector graphics to video frames, and assemble a valid H.264 MP4 in this environment entirely via code, since native AI video/audio generators are unavailable. PROBE for the strongest reachable programmatic generation tools (e.g., Python TTS libraries like pyttsx3 or gTTS, vector drawing libraries, and ffmpeg/moviepy). Record your findings in capabilities.md. Then, build a TINY but COMPLETE end-to-end tracer slice: a ~10 second video file named slice.mp4 containing one scene, one voiced line, one cut, and burned-in subtitles, assembled exactly the way the final task will assemble it. Judge it against the exemplars. Output manifest.md as an integration contract detailing file paths, formats, and a tiny sample proving format. SHARED SPEC: 1080x1920 vertical format, 12-15 fps. Comedy style: South Park/Family Guy spirit (edgy, absurdist, entirely original IP). Visuals: Flat, geometric characters (circles/polygons, thick black outlines) animated purely by translation (hopping) and binary mouth states. Voices: Programmatic TTS with deadpan delivery. Output: H.264/AAC mp4 with burned-in subtitles. Obey the GOAL CONSTITUTION provided in artifacts_needed, using its rubric as the judging standard. JUDGING: Mechanical facts: slice.mp4, capabilities.md, and manifest.md exist; video decodes, duration is ~10s, contains audio and video streams. Subjective: 8.0/10 on the GOAL CONSTITUTION rubric. verify.py must consume the deliverable the way its audience will - decode and sample its ACTUAL content, failing degenerate output (blank, silent, empty, uniform, truncated) regardless of metadata - and for perceptual deliverables must include one cheap model-perception call confirming the sampled content actually depicts what the task requires (a geometric character and subtitles). Placeholder or stub content anywhere in the deliverable is an automatic FAIL at any threshold.

Save the main deliverable as slice.mp4.

Already provided in your working directory: artifacts/board-20260902-201456/constitution.md

SWARM CHANNEL: you are working issue #24 of the GitHub repo drpokerface/open (token in GITHUB_TOKEN env). If you discover work this plan is missing, you may post ONE comment on your own issue via the API starting exactly 'PROPOSE-TASK: ' (state: title, why, which existing deliverable it unblocks, what it produces). Facing an irreversible, genuinely ambiguous choice, you may post ONE comment starting exactly 'QUESTION: ', then continue on the reversible path without waiting. If a PROVIDED input artifact fails your validation (placeholder, degenerate, or broken contract), post ONE comment starting exactly 'INPUT-REJECT: #<producing issue number> ' plus one line of evidence - the swarm will reopen that task; then declare impossible honestly instead of building on garbage. If providers.md lacks an adequate tool for a capability this task needs, you may post ONE comment starting exactly 'PROPOSE-PROVIDER: ' (capability, best candidate service, pricing, what it unlocks) - the owner decides about subscribing; continue meanwhile on the best reachable tier. Never create issues yourself; an owner-side arbiter reviews and answers as an 'ARBITER re' comment on this issue.
