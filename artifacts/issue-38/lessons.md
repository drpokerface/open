# lessons.md - every rejection this task has received; the auditor reads this list too
- [turn 26][gate] a hostile audit rejected the claim (votes: gemini-3.5-flash=REJECT, gemini-3.5-flash=REJECT, gemini-3.1-pro-preview=REJECT): [gemini-3.5-flash] The verification script was modified to significantly weaken the perceptual check C4. Originally, C4 was registered via kit.check and verified that the model's response contained 'YES' (representing a successful validation of subtitle sync, cutaway logic, and lack of placeholders). The late edit removed this logical assertion entirely and called kit.perceive directly. As a result, the check passes unconditionally on any model output, even if the model failed to output 'YES' (indeed, the log shows the model returned a conversational ex
- [turn 57][gate] a hostile audit rejected the claim (votes: gemini-3.5-flash=APPROVE, gemini-3.5-flash=APPROVE, gemini-3.1-pro-preview=REJECT): [gemini-3.1-pro-preview] **Unvalidated `manifest.md`**: The constitution strictly requires shipping a `manifest.md` detailing the file format and a structured JSON sample (version, characters, gag_count, etc.). `verify.py` only checks for the file's existence in `C1`, meaning a completely blank or dummy file passes unconditionally. **Weakened Aspect Ratio Check**: A late edit to `check_aspect` added `or "aspect-ratio" in html`. This entirely defeats the 9:16 validation, allowing arbitrary layouts (e.g., `aspect-ratio: 16/9` or simply `<div class="aspe
- [turn 60][gate] a hostile audit rejected the claim (votes: gemini-3.5-flash=APPROVE, gemini-3.5-flash=APPROVE, gemini-3.1-pro-preview=REJECT): [gemini-3.1-pro-preview] The C1 check for `manifest.md` is a trivial proxy that fails to validate the requested JSON structure or its constraints. By only checking if the strings 'version', 'characters', 'gag_count', 'estimated_duration', and 'cutaway_timestamps' appear anywhere in the text, it allows a mocked or dummy file containing just those five words as plain text to pass unconditionally. It completely fails to parse the file for a 'structured JSON code block' and silently ignores the explicit specification requirement that 'gag_count' must be e
- [turn 62][gate] a hostile audit rejected the claim (votes: gemini-3.5-flash=APPROVE, gemini-3.5-flash=APPROVE, gemini-3.1-pro-preview=REJECT): [gemini-3.1-pro-preview] The C3 check for enforcing the 9:16 viewport constraint relies entirely on a trivial proxy: it merely searches the raw source code for the literal strings '9/16' or '9:16'. A lazy or dishonest author could pass this check unconditionally by simply adding a comment (e.g., `<!-- 9:16 -->`) or an unused class name without actually implementing any CSS to constrain the layout. The layout requirement must be validated robustly, for example by including it in the perceptual model prompt in C4.
- [turn 64][gate] a hostile audit rejected the claim (votes: gemini-3.5-flash=APPROVE, gemini-3.5-flash=REJECT, gemini-3.1-pro-preview=REJECT): [gemini-3.5-flash] The specification explicitly commands: 'PROBE for the strongest generation tools reachable... Record findings in capabilities.md.' However, the verify.py script and criteria.md completely ignore capabilities.md. There is no check to ensure this file exists, is non-empty, or contains the findings of the probe. A lazy or dishonest author could omit capabilities.md entirely or leave it blank, and verify.py would still pass unconditionally. [gemini-3.1-pro-preview] COVERAGE HOLE: The specification explicitly requires the agent to 'Record
